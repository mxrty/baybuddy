{
  "name": "baybuddy",
  "version": "1.0.0",
  "description": "**Your smart eBay companion** — a Chrome extension that makes eBay search smarter with powerful filters, sold price analytics, and sticky search preferences.",
  "main": "content.js",
  "scripts": {
    "test": "jest",
    "test:pricing": "jest src/pricing",
    "build": "esbuild src/content.ts --bundle --outfile=content.js && esbuild src/popup.ts --bundle --outfile=popup.js && tailwindcss -i src/popup.css -o popup.css --minify",
    "watch:content": "esbuild src/content.ts --bundle --outfile=content.js --watch",
    "watch:popup": "esbuild src/popup.ts --bundle --outfile=popup.js --watch",
    "watch:css": "tailwindcss -i src/popup.css -o popup.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.1.42",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.9.1",
    "esbuild": "^0.28.0",
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1",
    "jsdom": "^27.0.1",
    "tailwindcss": "^3.4.19",
    "ts-jest": "^29.4.10",
    "typescript": "^6.0.3"
  }
}
